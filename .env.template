# LLM Provider: "gemini", "ollama", "groq"
LLM_PROVIDER=ollama

# API Key (required for gemini/groq, not needed for ollama)
LLM_API_KEY=

# Model name
LLM_MODEL=llama3.2

# Ollama base URL (only if using ollama)
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Server port
PORT=8000
